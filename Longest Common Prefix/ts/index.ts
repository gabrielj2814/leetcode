function longestCommonPrefix(strs: string[]): string {

    if(strs.length===0){
        return ""
    }
    let prefijo:string = strs[0]
    // for (let index = 1; index < array.length; index++) {
    //     const element = array[index];
        
    // }

    return ""
};

let lista:string[] = ["flower","flow","flight"]
longestCommonPrefix(lista)